<footer class="site-footer no-print">
  <span><strong>{{ footer.workbook_version }}</strong></span>
  <span>Extracted {{ footer.extraction_timestamp }}</span>
  <span>Source: {{ footer.source_file }}</span>
  <span>{{ footer.n_countries }} countries · {{ footer.n_milestones }} milestones · {{ footer.n_progress }} tracked rows · {{ footer.n_results }} indicator&times;country rows</span>
  <span>Reporting period: {{ footer.reporting_period }}</span>
  <span>Built as a single self-contained file — no external network calls at runtime.</span>
</footer>
